[build]
builder = "RAILPACK"
buildCommand = "pnpm install && pnpm run build"

[deploy]
startCommand = "bash start.sh"
restartPolicyType = "ON_FAILURE"
healthCheckPath = "/api/health" # Opcional: si tienes una ruta de salud